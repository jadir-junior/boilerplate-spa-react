import React from 'react'
import { render, screen } from '@testing-library/react'

import {{pascalCase name}} from './{{pascalCase name}}'

describe('<{{pascalCase name}} />', () => {
  it('should render then {{pascalCase name}}', () => {
    render(<{{pascalCase name}} />)
  })
})